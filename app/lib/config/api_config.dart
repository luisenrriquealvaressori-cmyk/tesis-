/// Configuración global de la API del servidor.
class ApiConfig {
  ApiConfig._();

  /// URL base de la API backend en producción (Render).
  static const String baseUrl = 'https://tesis-api-t5zw.onrender.com';
}
